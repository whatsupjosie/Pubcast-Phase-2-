# PubCast AI v5.0 — Dependencies
# Install: pip install -r requirements.txt

# Core framework
fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
starlette>=0.35.0,<1.0
pydantic>=2.0,<3.0

# HTTP client (Ollama, external APIs)
httpx>=0.27.0,<1.0

# CORS
python-multipart>=0.0.7

# LLM providers (install as needed)
# openai>=1.12.0        # Uncomment for OpenAI bots
# google-generativeai   # Uncomment for Gemini bots

# Database
aiosqlite>=0.19.0

# File handling
python-magic>=0.4.27;sys_platform!="win32"

# Flask (for standalone Iteration Wallet server — optional)
# flask>=3.0
# flask-cors>=4.0
